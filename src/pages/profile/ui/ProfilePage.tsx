// ui

// styles
import styles from './ProfilePage.module.scss';

export const ProfilePage = () => {
    return (
        <main className={styles.profile}>
            <h1>Profile</h1>
        </main>
    );
};
